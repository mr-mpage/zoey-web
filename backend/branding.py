"""Forker's edit-here file.

Server-side strings that name the baby — push notification titles, report
headers. Centralised so a fork has one place to change. The companion
file is `frontend/src/lib/branding.ts` for client-side strings.
"""

BABY_NAME = "Zoey"
