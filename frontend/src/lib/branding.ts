/** Forker's edit-here file.
 *
 * Structural strings — what the app calls itself, the family. Centralised
 * so a fork has one obvious place to change. The hand-crafted encouragement
 * strings in `encouragement.ts` are intentionally NOT here: they read as
 * one voice referencing the baby by name, and a {placeholder} would gut
 * their feel. Replace those with sed when you fork.
 */

export const BABY_NAME = 'Zoey'
export const PARENT_NAMES = 'Max and Sabrina'
export const COPYRIGHT_HOLDER = 'The Page Family'
